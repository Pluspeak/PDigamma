% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/3_model_selection.R
\name{fit_generalized_zeta}
\alias{fit_generalized_zeta}
\title{Fit Generalized Zeta Submodel (rho = 0)}
\usage{
fit_generalized_zeta(
  y,
  X = NULL,
  Z = NULL,
  theta_init = NULL,
  delta_min = 1.05,
  max_iter = 1000,
  tol = 1e-06,
  verbose = FALSE
)
}
\arguments{
\item{y}{Vector of observed counts}

\item{X}{Design matrix for alpha (NULL for intercept-only)}

\item{Z}{Design matrix for delta (NULL for intercept-only)}

\item{theta_init}{Initial values (typically from full model fit or initialization)}

\item{delta_min}{Hard boundary for delta (default: 1.05)}

\item{max_iter}{Maximum EM iterations (default: 1000)}

\item{tol}{Convergence tolerance (default: 1e-6)}

\item{verbose}{Print progress (default: FALSE)}
}
\value{
Fitted model object of class pdigamma_fit with rho fixed at 0
}
\description{
Fits restricted model with rho fixed at 0, corresponding to limit as rho -> 0+.
This is the boundary case of PDigamma family.
}
\details{
Generalized Zeta distribution: q(x) = (alpha)_x / [zeta(delta; alpha) * Gamma(alpha+1) * x^delta]
  where zeta(delta; alpha) is the Hurwitz zeta function.
  
  This is the boundary case rho = 0 of PDigamma family (Section 2.4.1).
}
\references{
Section 2.4.1 (Generalized Zeta Boundary), Proposition 3.5
}
\seealso{
boundary_lrt, fit_em
}
